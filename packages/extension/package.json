{
  "name": "@llm-proxy/extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ && echo 'Extension built to dist/'",
    "dev": "nodemon --watch src --exec yarn build",
    "test": "echo 'No tests implemented'",
    "lint": "eslint src",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "@llm-proxy/shared": "1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
